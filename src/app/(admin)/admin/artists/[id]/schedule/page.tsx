import { createPrivatePageMetadata } from "@/core/seo/metadata";
export { default } from "@/admin/pages/artists/[id]/schedule/page";
export const metadata = createPrivatePageMetadata("Artist Schedule");
